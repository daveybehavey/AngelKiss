import { HomeMarketGalleryClient } from "@/components/storefront/home-market-gallery-client";

/** Pop-ups & markets booth carousel (client: swipe, arrows, dots). */
export function HomeMarketGallery() {
  return <HomeMarketGalleryClient />;
}
